<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="../../public/images/log-modified.png">
    <title>Chat Bot</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <script defer src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script> 
    <script src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/4.0.1/socket.io.js"></script>
<style>
  *{
    font-family: 'Michroma', sans-serif;
  }
  .cards {
  display: flex;
  flex-direction: column;
  gap: 15px;
}

.cards .red {
  background-color: #000000;
}


.cards .card {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  text-align: center;
  height: 70px;
  width: 250px;
  border-radius: 10px;
  color: white;
  cursor: pointer;
  transition: 400ms;
}

.cards .card p.tip {
  padding-top: 10px;
  margin-bottom: -6px;
  font-size: 20px;
  font-weight: 700;
}

.cards .card p.second-text {
  font-size: .7em;
}

.cards .card:hover {
  transform: scale(1.1, 1.1);
}

/* .cards:hover > .card:not(:hover) {
  filter: blur(10px);
  transform: scale(0.9, 0.9);
} */
a{
  text-decoration: none !important;
}

.users{
  display: flex;
  justify-content: center;
  align-items: center;
  flex-wrap: wrap;
}
</style>
</head>
<body>
  <%- include('./layout/adminHeader')-%>
   <h3 class="text-center" style="margin-top: 20px;   text-transform: capitalize; ">Total Chats: <%=locals?.clients.length%> <span id="totalUsers"></span></h3>
      <div class="container">
        <div class="users mb-5" style="margin-top: 50px; gap: 15px;">
          <%if(locals?.clients.length>0){%>
            <%clients.forEach(chat=>{%>
              <a href="/openUserChat?id=<%=chat.userId%>" id="">
              <div class="cards">
                <div class="card red">
                      <p class="tip" style="text-transform: capitalize;"><%=chat.name%></p>
                      <p class="second-text mt-1 text-danger"><%=chat.count%> messages</p>
                </div>
            </div></a>
              <%})%>
          <%} else {%>
            <div class="cards">
              <div class="card red pb-3">
                    <p class="tip" style="text-transform: capitalize;">No Chat's</p>
              </div>
            <%}%>
        </div>
        </div>
      </div>
        <%- include('./layout/footer')-%>
</body>
</html>
